import AppKit

extension NSFont {
    static let codeEditor = NSFont.monospacedSystemFont(ofSize: 13, weight: .regular)
    static let lineNumber = NSFont.monospacedSystemFont(ofSize: 11, weight: .regular)
}
